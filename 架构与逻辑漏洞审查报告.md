# 医疗 Agent 项目架构与逻辑漏洞审查报告

**审查范围**：`src/medical_agent/` 全部 17 个源文件
**审查日期**：2026-07-26

---

## 一、架构总览

项目采用"证据中心"的 Plan-Execute DAG 架构，核心数据流为：

```
HTTP 请求 → MedicalAgentService.run/chat
  → plan_validator 校验任务 DAG
  → execute_dag (ThreadPoolExecutor 并行调度)
  → 每个任务 worker：检索 → 抽取 → 总结 (ThreeStageTaskAgent)
  → EvidenceRegistry 注册证据 (P# 患者事实 / K# 知识库事实)
  → evaluator 结论级校验
  → repair 定向修复 (最多 2 轮)
  → render_report + 归档
```

整体设计思路清晰，安全边界划分得当（模型不生成 ID/URL/状态，服务端全权校验；审计事件严格脱敏；凭证不落日志）。但在并发安全与若干边界条件上存在逻辑漏洞。

---

## 二、确认的逻辑漏洞

### 【高】1. EvidenceRegistry 线程不安全 — 并发竞态导致证据 ID 冲突

**位置**：`src/medical_agent/evidence.py` 第 20-52 行，`_add` 方法

**问题**：
`EvidenceRegistry` 没有任何锁保护，但被并发的 DAG 任务 worker 通过 `ThreeStageTaskAgent._retrieve`（`agent_pipeline.py` 第 70-111 行）**并发访问**。

```python
# evidence.py 第 33-39 行 —— 无锁的读-改-写
existing_id = self._dedupe.get(key)        # 读
if existing_id:
    return deepcopy(self._items[existing_id])
self._counters[kind] += 1                  # 非原子：read-modify-write
prefix = "P" if kind == "patient" else "K"
evidence_id = f"{prefix}{self._counters[kind]}"
```

`self._counters[kind] += 1` 在 CPython 下编译为多条字节码（LOAD→BINARY_SUBSCR→BINARY_ADD→STORE_SUBSCR），GIL 可在中间释放。

**触发路径**（确凿）：
- `dag_scheduler.execute_dag`（第 124-125 行）用 `ThreadPoolExecutor` **并行执行同一波次**的就绪任务。
- `DemoModelAdapter.plan` 生成的 5 步计划中，**Task 3 和 Task 4 依赖相同（均为 [1, 2]）**，因此处于同一执行波次，被并行调度。
- 两者都会在 `agent.run → _retrieve` 中调用 `registry.add_patient / add_knowledge`。

**后果**：
两个并发线程可能读到相同计数器值，生成**重复的证据 ID**（如两个 "P6"），导致：
- `self._items` 字典中后写入覆盖先写入 → 证据丢失
- 引用校验（`evaluator` 的 `ref in evidence_by_id`）指向错误证据
- **证据链可追溯性被破坏** —— 对医疗证据完整性是核心缺陷

**对比**：`retrieval.py` 的 `JsonKnowledgeBase` 正确使用了 `RLock`（第 93 行），`run_archive.py` 也用 `RLock`（第 188 行），唯独 `EvidenceRegistry` 缺失同步，存在明显的不一致。

**验证说明**：在纯内存场景下 CPython 3.13 的 GIL 使该竞态难以稳定复现（我构造了 200 线程 Barrier 同步测试，未触发重复），但这不改变代码缺陷性质。在接入真实模型（`AliyunCompatibleModelAdapter` 的 `urlopen` 网络 IO 会释放 GIL，线程切换更频繁）或未来无 GIL Python（PEP 703）下，触发概率显著上升。

**修复建议**：
```python
from threading import RLock

class EvidenceRegistry:
    def __init__(self) -> None:
        self._items: dict[str, dict[str, Any]] = {}
        self._dedupe: dict[tuple[str, str, str, str], str] = {}
        self._counters = {"patient": 0, "knowledge": 0}
        self._lock = RLock()          # 新增

    def _add(self, *, kind, text, source, locator, document_id, metadata=None):
        with self._lock:              # 包裹整个临界区
            # ...原有逻辑...
```

---

### 【中】2. chat() 双重归档

**位置**：`src/medical_agent/service.py` 第 522-535 行

**问题**：
`chat()` 调用 `run()`，而 `run()` 内部 `finish()` 已调用 `self._archive_result(result)`（第 592 行）。随后 `chat()` 在第 535 行**再次调用** `self._archive_result(result)`：

```python
def chat(self, ...) -> dict[str, Any]:
    result = self.run(...)              # run 内部已归档
    result["answer"] = self._chat_answer(...)
    result["mode"] = "patient" if ... else "general"
    self._archive_result(result)        # ← 第二次归档（冗余）
    return result
```

**影响**：功能无害（第二次覆盖第一次，目的是让证据页带上 chat 专属的 answer/mode 字段），但浪费资源、语义模糊。

**修复建议**：让 `run()` 在 chat 场景下跳过归档，或由 `chat()` 在 `run()` 返回后统一归档一次。

---

### 【中】3. 修复重跑无法解决证据缺失根因

**位置**：`src/medical_agent/repair.py` + `service.py` 第 743-782 行

**问题**：
当 `evaluator` 发现 `MISSING_PATIENT_REF`（结论缺少 P# 引用）时，`build_repair_plan` 将该结论所属 task 及其后代加入 `rerun_tasks`。但如果**根因是检索阶段未检索到患者事实**（证据本身缺失），重跑同一 task 用相同查询大概率仍检索不到，修复无效。

**影响**：修复轮次被浪费，最终转人工。有 `max_repair_rounds=2` 兜底，不会死循环，属于设计权衡。可改进方向：修复时换用不同查询策略，或对 `MISSING_*` 类问题扩大检索范围。

---

## 三、低严重度 / 健壮性建议

### 4. plan_validator 允许无效任务进入 tasks 列表

**位置**：`plan_validator.py` 第 150 行

即使 task id 不连续（有校验错误），只要 `isinstance(task_id, int) and isinstance(goal, str)` 仍会 `append` 到 `tasks`。所幸第 153-154 行 `if validation_errors: return invalid` 兜底，脏数据不会被使用。建议在存在 id 错误时提前 `continue`，避免构建半成品列表。

### 5. evaluator 中 dual-support 检查后不跳过 model 判定

**位置**：`evaluator.py` 第 75-89 行

`MISSING_PATIENT_REF`/`MISSING_KB_REF` 记录后不 `continue`，继续调用 `model.judge_claim`，若返回 `NOT_SUPPORTED` 会叠加 issue。这是有意设计（可同时记录多类问题），但可能导致 issue 数虚高，影响 `repair["repairable"]` 判定。建议明确注释或按需跳过。

### 6. evaluator 不检查重复 claim_id

**位置**：`evaluator.py` 第 35-37 行

若模型返回重复的 claim `id` 字段，`issues`/`judgements` 中 `claim_id` 会混淆。实际 `service._collect_claims` 会重新分配 `C{counter}`（第 380 行），覆盖模型给的 id，实践不冲突。非真正漏洞。

### 7. aliyun_model 的 judge_claim 子串匹配顺序

**位置**：`aliyun_model.py` 第 249-251 行

```python
for verdict in ("NOT_SUPPORTED", "SUPPORTED", "UNCERTAIN"):
    if verdict in response:
        return verdict
```

`NOT_SUPPORTED` 优先匹配。若模型返回包含多个 verdict 的文本会取第一个。`max_tokens=20` 限制了这种情况，影响可控，但可改为正则全词匹配更稳健。

---

## 四、已确认的良好实践（非漏洞）

| 维度 | 实现 | 评价 |
|------|------|------|
| 路径遍历防御 | `server.py` 第 276-281 行 `resolve()` + `relative_to()` | 安全 |
| 请求体大小限制 | `MAX_BODY_BYTES = 2MB` | 合理 |
| SSRF 防护 | base_url 仅来自环境变量/本地配置；`normalize_base_url` 校验 http/https 前缀 | 安全 |
| 提示注入防御 | `model_view` 只暴露 id/text/source；`_valid_result` 过滤伪造 refs；system prompt 限制模型行为 | 良好 |
| 审计脱敏 | `redact_audit_event` 严格白名单，不记录病历/模型响应/密钥/思维链 | 良好 |
| 凭证保护 | API key 不写入日志/响应/审计；provider 错误不含凭证 | 良好 |
| DAG 防环 | `plan_validator` 限制依赖只能指向更小 id，弱模型无法构造环 | 巧妙 |
| 失败传播 | 上游 failed/blocked 时下游标记 blocked，不带着不完整上下文运行 | 正确 |
| 归档 TTL | 进程内内存 + 30 分钟过期 + 最多 24 条，重启即失效 | 符合最小留存 |
| 证据去重 | `_dedupe` 按 (kind, text, source, locator) 去重 | 合理（但需配锁） |

---

## 五、问题汇总

| # | 严重度 | 问题 | 文件 |
|---|--------|------|------|
| 1 | 高 | EvidenceRegistry 无锁并发竞态 | evidence.py |
| 2 | 中 | chat() 双重归档 | service.py |
| 3 | 中 | 修复重跑无法解决证据缺失根因 | repair.py |
| 4 | 低 | plan_validator 无效任务进入列表 | plan_validator.py |
| 5 | 低 | evaluator dual-support 不跳过 | evaluator.py |
| 6 | 低 | evaluator 不查重 claim_id | evaluator.py |
| 7 | 低 | judge_claim 子串匹配顺序 | aliyun_model.py |

**优先级**：建议立即修复 #1（EvidenceRegistry 加锁），它是唯一可能破坏医疗证据完整性的真实缺陷。其余为健壮性改进。
