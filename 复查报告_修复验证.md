# 医疗 Agent 复查报告 — 修复验证

**复查性质**：基于当前最新代码的二次审查
**复查日期**：2026-07-26（首次审查同日，约 1 小时后）

---

## 复查背景

文件修改时间检测显示：首次审查（11:20）之后，以下 **8 个文件于 12:07–12:19 被修改**（对应根据首次报告所做的修复）：

`evidence.py`、`service.py`、`repair.py`、`agent_pipeline.py`、`plan_validator.py`、`aliyun_model.py`、`evaluator.py`、`server.py`

本次复查针对这些变更逐一验证修复质量，并重新排查遗漏。

---

## 一、首次发现问题 → 本次验证结果

| 首次严重度 | 问题 | 本次状态 | 修复方式验证 |
|-----------|------|---------|-------------|
| **高** | EvidenceRegistry 并发竞态 | ✅ **已修复** | `evidence.py` 引入 `RLock`，整个 `_add` 及 `get/all/as_map/model_view` 全部置于锁内 |
| **中** | chat() 双重归档 | ✅ **已修复** | `run()` 新增 `archive_result: bool` 参数；`chat()` 传 `archive_result=False`，仅在末尾统一归档一次 |
| **中** | 修复重跑无法解决证据缺失根因 | ✅ **已修复** | `repair._source_task_ids` 定位检索根任务 + `task_directives` 下发 + `agent.run` 按 `repair_codes` 生成扩大化查询 |
| **低** | plan_validator 无效任务进入列表 | ✅ **已修复** | 新增 `error_count_before_task` 守卫，仅无字段级错误时才 append |
| **低** | judge_claim 子串匹配顺序 | ✅ **已修复** | 改为 `re.fullmatch` 正则全词匹配 |
| **低** | evaluator dual-support 不跳过 | ✅ **已澄清** | 注释明确为"独立 gate"有意设计，配合新 repair 路由 |
| **低** | evaluator claim_id 不查重 | ⚠️ **遗留（低风险）** | `service._collect_claims` 重新分配 `C{counter}` 覆盖模型 id，实践中不冲突 |

---

## 二、逐项修复验证细节

### 1. EvidenceRegistry 加锁（首次高严重度）

```python
# evidence.py 第 22 行 + 第 34 行
self._lock = RLock()           # 注释明确：保护去重查找、ID分配、插入为原子操作
def _add(...):
    with self._lock:           # 包裹整个临界区
```

`get()`、`all()`、`as_map()`、`model_view()` 也全部加锁。**修复正确、彻底。**

### 2. 并发修复的实测验证

针对"并发路径是否真实存在"与"修复后是否安全"做了三组实测：

- **波次实测**：demo 计划波次 = `[[1],[2],[3,4],[5]]`，Task3 与 Task4 同波次并行，实测峰值并发 worker = 2。**并发路径真实存在，加锁确有必要。**
- **压力测试**：200 线程 Barrier 同步、32000+ 次注册、`sys.setswitchinterval` 调至最小 → 无重复 ID、无去重失效。
- **IO 场景**：worker 先 10ms IO（释放 GIL）再注册 → 无冲突。

**一个重要澄清**：复查中途我曾一度误判"CPython 的 GIL 使无锁代码也安全"，但随后通过文件修改时间确认——**`evidence.py` 此时已被加锁**。测试"未复现问题"是因为代码已被修复，而非 GIL 天然保护。因此首次"高严重度并发"的判断是**正确且必要**的，加锁修复是恰当的。

### 3. 修复根因链路（首次中严重度）

修复后的完整链路已闭合：

```
repair.build_repair_plan
  → _source_task_ids 定位"患者检索/知识检索"根任务，纳入 failed_roots
  → 生成 task_directives（含 issue_codes）
service → agent.set_repair_directives(task_directives)
agent.run → 按 repair_codes 生成扩大化查询：
  MISSING_PATIENT_REF → "患者病历关键事实 {goal}"
  MISSING_KB_REF      → "医学知识库直接依据 {goal}"
  NO_REF/BAD_REF/NOT_SUPPORTED → "直接支持当前结论的证据 {goal}"
```

不再是"相同查询空转"，而是**重开检索根 + 换用扩大化查询**，能真正触达缺失证据。

### 4. 线程安全复核（新增代码）

`agent.set_repair_directives` 在主线程 repair 循环中写 `self._repair_codes`（整体替换引用，原子），worker 线程在 `execute_dag` 启动后读。`ThreadPoolExecutor` 的 with 块保证主线程等待 worker 完成，时序安全 + dict 操作原子，**无新并发问题**。

---

## 三、新增能力（非问题，确认无缺陷）

- **多模型配置**：`model_profiles` / `model_catalog()` / `_select_model()`，server 端点透传 `model_profile`。
- **`_select_model` 调用时机安全**：在 `run_id` 生成与 `archive.start` 之前执行；无效 profile 提前抛 `ValueError`，由 server 捕获返回 400，不残留空归档。
- **`OpenAICompatibleModelAdapter`**：原类加 provider 参数，支持阿里云/OpenRouter/OpenAI 兼容端点；旧类名保留别名兼容。

---

## 四、结论

1. **首次报告的全部高/中严重度问题均已正确修复**，多数低严重度项也已处理或澄清。
2. 修复质量高：加锁到位、修复链路闭合、参数守卫严谨、正则匹配精确。
3. 经实测与逐行复核，**未发现新的逻辑漏洞**。
4. 唯一遗留项为 evaluator claim_id 不查重（低风险，`service` 层重分配 id 已兜底），可不必处理。

**整体评价**：架构清晰、安全边界严格（提示注入防御、审计脱敏、凭证保护、DAG 防环、归档 TTL 均到位），并发安全已补齐，代码处于健康状态。
